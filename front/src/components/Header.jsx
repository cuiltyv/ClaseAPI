import React from "react";

const Header = () => {
  //Header basico con estilo
  const textStyle = {
    color: "green",
    fontSize: 40,
    fontFamily: "Arial",
  };
  return <h1 style={textStyle}>Notes</h1>;
};

export default Header;
